import { NesrineanesPipe } from './nesrineanes.pipe';

describe('NesrineanesPipe', () => {
  it('create an instance', () => {
    const pipe = new NesrineanesPipe();
    expect(pipe).toBeTruthy();
  });
});
